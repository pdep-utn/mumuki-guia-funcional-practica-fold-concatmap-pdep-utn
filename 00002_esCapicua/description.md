Usando las funciones `foldl` y `concatMap`, definir la función `esCapicua/1`, que dada una lista de listas, me devuelve si la concatenación de las sublistas es una lista capicua.

```
Main> esCapicua ["ne","uqu","en"]
True
```
